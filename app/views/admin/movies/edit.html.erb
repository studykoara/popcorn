<% @page_title = "映画情報の編集" %>

<h1><%= @page_title %></h1>

<div class="toolbar"><%= link_to "映画の詳細に戻る", [:admin, @movie] %></div>

<%= form_for [:admin, @movie] do |form| %>
    <%= render "shared/movie_form", form: form %>
    <div><%= form.submit %></div>
<% end %>